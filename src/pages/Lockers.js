import { Typography } from '@mui/material';
import LockerTable from '../components/LockerTable';
import NewLocker from '../components/NewLocker';

const Lockers = () => {
  return (
    <>
      <Typography variant="h6">Locker Details</Typography>
    </>
  );
};

export default Lockers;
